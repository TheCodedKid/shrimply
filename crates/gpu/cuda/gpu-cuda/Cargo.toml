[package]
name = "shrimply-gpu-cuda"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
half.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
libc.workspace = true

[build-dependencies]
cc.workspace = true
