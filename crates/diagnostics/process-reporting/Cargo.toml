[package]
name = "shrimply-process-reporting"
version = "0.1.0"
edition = "2024"

[dependencies]
libc.workspace = true
tracing.workspace = true
tracing-subscriber = { workspace = true, features = ["env-filter"] }
